import { PosterbackdropPipe } from './posterbackdrop.pipe';

describe('PosterbackdropPipe', () => {
  it('create an instance', () => {
    const pipe = new PosterbackdropPipe();
    expect(pipe).toBeTruthy();
  });
});
